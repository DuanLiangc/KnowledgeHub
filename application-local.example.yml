# 复制为 application-local.yml 后填写本机配置。
# application-local.yml 已被 Git 忽略，不会进入版本库。
knowledge-hub:
  ai:
    openai:
      api-key: your_api_key_here
      base-url: https://api.openai.com/v1
      model: gpt-5.6-luna
